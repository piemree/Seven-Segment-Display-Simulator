import React from "react";
import c from "classnames";

type DisplayProps = {
//   isVisible: boolean;
//   setIsVisible: (isVisible: boolean) => void;
//   color: string;
//   isVertical: boolean;
  className: string;
};

export default function Display(props: DisplayProps) {
  return <div className="display is-vertical"></div>;
}
